@model PrivateTutorCenter.Models.StudentRegisterViewModel
@{
    ViewBag.Title = "Register as";
}
<h2>@ViewBag.Title:</h2>
<br />

    <div class="form-group">

        <div class="col-sm-2">
            <a href="@Url.Action("StudentRegister","Account")" class="btn btn-lg btn-info">Student</a>
        </div>
        <div class="col-sm-10">
            <a href="@Url.Action("TeacherRegister","Account")" class="btn btn-lg btn-primary">Teacher</a>
        </div>
    </div>

<hr /> 

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
